extends Control

@onready var camera_2d = $"../Player/Camera2D"
@onready var control = $"."

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
	#control.position = camera_2d.position
